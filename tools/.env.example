# Copy this file to tools/.env and fill in your key.
# tools/.env is gitignored — the key never reaches the repo or the browser.
# It's only used on your machine, by build-audio.mjs, to generate mp3 files.

OPENAI_API_KEY=sk-...
